kind: DeploymentConfig
apiVersion: apps.openshift.io/v1
metadata:
  name: products-ms
spec:
  strategy:
    type: Recreate
    recreateParams:
      timeoutSeconds: 600
    activeDeadlineSeconds: 21600
  triggers:
    - type: ImageChange
      imageChangeParams:
        automatic: true
        containerNames:
          - products-ms
        from:
          kind: ImageStreamTag
          namespace: dev
          name: 'product-ms:latest'
    - type: ConfigChange
  replicas: 1
  revisionHistoryLimit: 10
  test: false
  selector:
    app: products-ms
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: products-ms
      annotations:
        app.openshift.io/runtime: nodejs
    spec:
      containers:
        - resources:
            limits:
              memory: 512Mi
          securityContext:
            privileged: false
          ports:
            - containerPort: 8080
              protocol: TCP
              name: http
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: products-ms-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: products-ms-secret
                  key: DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: products-ms-secret
                  key: DB_NAME
          imagePullPolicy: IfNotPresent
          name: products-ms
          volumeMounts:
            - name: products-ms-config
              mountPath: /config
      volumes:
        - name: products-ms-config
          configMap:
            name: products-ms-config
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst